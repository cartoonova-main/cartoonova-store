<style>
  #shopify-section-{{ section.id }} {
    --hero-bg: {{ section.settings.background | default: '#ffffff' }};
    --hero-text: {{ section.settings.text_color | default: '#333333' }};
    --hero-title: {{ section.settings.title_color | default: '#1a1a1a' }};
    --hero-highlight: {{ section.settings.highlight_color | default: '#FFD800' }};
    --hero-btn-bg: {{ section.settings.button_background | default: '#FFD800' }};
    --hero-btn-text: {{ section.settings.button_text_color | default: '#1a1a1a' }};
  }

  .hero-with-image {
    background: var(--hero-bg);
    color: var(--hero-text);
    padding: var(--spacing-12) 0;
    position: relative;
    overflow: hidden;
  }

  .hero-with-image__container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 var(--container-gutter);
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-10);
    align-items: center;
  }

  @media screen and (min-width: 990px) {
    .hero-with-image__container {
      grid-template-columns: 1fr 1fr;
      gap: var(--spacing-16);
    }

    .hero-with-image {
      padding: var(--spacing-16) 0;
    }
  }

  .hero-with-image__content {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-6);
  }

  .hero-with-image__eyebrow {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-2);
    font-size: var(--text-sm);
    color: {{ section.settings.eyebrow_color | default: '#b45309' }};
    font-weight: 700;
    background: linear-gradient(135deg, #FFD800 0%, #FFA500 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .hero-with-image__title {
    font-size: clamp(2.2rem, 5vw, 4rem);
    font-weight: 900;
    line-height: 1.05;
    color: var(--hero-title);
    margin: 0;
    text-shadow: 2px 2px 0 rgba(0,0,0,0.05);
  }

  .hero-with-image__title-highlight {
    color: var(--hero-highlight);
    font-style: italic;
    display: block;
    text-shadow: 3px 3px 0 rgba(0,0,0,0.1);
    position: relative;
  }

  .hero-with-image__description {
    font-size: var(--text-lg);
    line-height: 1.7;
    color: var(--hero-text);
    opacity: 0.9;
    max-width: 500px;
  }

  .hero-with-image__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-3);
    background: var(--hero-btn-bg);
    color: var(--hero-btn-text);
    padding: 18px 36px;
    border-radius: 50px;
    font-weight: 800;
    font-size: 1.1rem;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    transition: transform 0.25s ease, box-shadow 0.25s ease;
    max-width: fit-content;
    box-shadow: 0 4px 0 rgba(0,0,0,0.2), 0 6px 20px rgba(0,0,0,0.1);
    border: 3px solid rgba(0,0,0,0.1);
  }

  .hero-with-image__button:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 0 rgba(0,0,0,0.15), 0 12px 30px rgba(0,0,0,0.15);
  }

  .hero-with-image__button:active {
    transform: translateY(-1px);
    box-shadow: 0 2px 0 rgba(0,0,0,0.2), 0 4px 10px rgba(0,0,0,0.1);
  }

  .hero-with-image__benefits {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-6);
    margin-top: var(--spacing-6);
    padding: var(--spacing-4) var(--spacing-6);
    background: rgba(255, 216, 0, 0.1);
    border-radius: 12px;
    border: 2px dashed rgba(255, 216, 0, 0.4);
  }

  .hero-with-image__benefit {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-2);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--hero-text);
  }

  .hero-with-image__benefit-icon {
    flex-shrink: 0;
    width: 22px;
    height: 22px;
    color: {{ section.settings.benefit_icon_color | default: '#FFD800' }};
    filter: drop-shadow(1px 1px 0 rgba(0,0,0,0.1));
  }

  .hero-with-image__image-wrapper {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-with-image__image-wrapper::before {
    content: '';
    position: absolute;
    top: -10px;
    left: -10px;
    right: -10px;
    bottom: -10px;
    background: linear-gradient(135deg, #FFD800 0%, #FFA500 50%, #FFD800 100%);
    border-radius: 20px;
    z-index: -1;
    opacity: 0.3;
    transform: rotate(-2deg);
  }

  .hero-with-image__image {
    max-width: 100%;
    height: auto;
    border-radius: 16px;
    box-shadow: 0 20px 60px rgba(0,0,0,0.15), 0 0 0 4px rgba(255,216,0,0.3);
    transition: transform 0.3s ease;
  }

  .hero-with-image__image-wrapper:hover .hero-with-image__image {
    transform: scale(1.02) rotate(1deg);
  }

  @media screen and (max-width: 989px) {
    .hero-with-image__content {
      text-align: center;
      align-items: center;
    }

    .hero-with-image__description {
      max-width: 100%;
    }

    .hero-with-image__benefits {
      justify-content: center;
    }

    .hero-with-image__image-wrapper::before {
      display: none;
    }
  }
</style>

<section class="hero-with-image">
  <div class="hero-with-image__container">
    <div class="hero-with-image__content">
      {%- if section.settings.eyebrow != blank -%}
        <p class="hero-with-image__eyebrow">
          {{ section.settings.eyebrow }}
        </p>
      {%- endif -%}

      {%- if section.settings.title != blank -%}
        <h1 class="hero-with-image__title">
          {{ section.settings.title }}
          {%- if section.settings.title_highlight != blank -%}
            <span class="hero-with-image__title-highlight">{{ section.settings.title_highlight }}</span>
          {%- endif -%}
        </h1>
      {%- endif -%}

      {%- if section.settings.description != blank -%}
        <p class="hero-with-image__description">
          {{ section.settings.description }}
        </p>
      {%- endif -%}

      {%- if section.settings.button_text != blank -%}
        <a href="{{ section.settings.button_url }}" class="hero-with-image__button">
          {{ section.settings.button_text }}
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      {%- endif -%}

      {%- if section.settings.benefit_1 != blank or section.settings.benefit_2 != blank or section.settings.benefit_3 != blank -%}
        <div class="hero-with-image__benefits">
          {%- if section.settings.benefit_1 != blank -%}
            <div class="hero-with-image__benefit">
              <svg class="hero-with-image__benefit-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <polyline points="12,6 12,12 16,14"/>
              </svg>
              <span>{{ section.settings.benefit_1 }}</span>
            </div>
          {%- endif -%}

          {%- if section.settings.benefit_2 != blank -%}
            <div class="hero-with-image__benefit">
              <svg class="hero-with-image__benefit-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                <polyline points="22,4 12,14.01 9,11.01"/>
              </svg>
              <span>{{ section.settings.benefit_2 }}</span>
            </div>
          {%- endif -%}

          {%- if section.settings.benefit_3 != blank -%}
            <div class="hero-with-image__benefit">
              <svg class="hero-with-image__benefit-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
                <circle cx="8.5" cy="8.5" r="1.5"/>
                <polyline points="21,15 16,10 5,21"/>
              </svg>
              <span>{{ section.settings.benefit_3 }}</span>
            </div>
          {%- endif -%}
        </div>
      {%- endif -%}
    </div>

    <div class="hero-with-image__image-wrapper">
      {%- if section.settings.image != blank -%}
        {%- capture sizes -%}(min-width: 990px) 50vw, 100vw{%- endcapture -%}
        {%- capture widths -%}400,600,800,1000,1200{%- endcapture -%}
        {{ section.settings.image | image_url: width: section.settings.image.width | image_tag: loading: 'eager', sizes: sizes, widths: widths, class: 'hero-with-image__image', fetchpriority: 'high' }}
      {%- else -%}
        {{ 'lifestyle-2' | placeholder_svg_tag: 'hero-with-image__image' }}
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero with image",
  "class": "shopify-section--hero-with-image",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow text",
      "default": "🎨 Hand-Drawn by Real Artists"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title (first part)",
      "default": "Turn Your Favorite People into"
    },
    {
      "type": "text",
      "id": "title_highlight",
      "label": "Title (highlighted part)",
      "default": "Yellow Cartoon Portraits"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "A real artist draws your custom portrait by hand, capturing personality and special details AI can't truly replicate."
    },
    {
      "type": "header",
      "content": "Button"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "Start My Portrait"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button URL"
    },
    {
      "type": "header",
      "content": "Benefits"
    },
    {
      "type": "text",
      "id": "benefit_1",
      "label": "Benefit 1",
      "default": "Preview in 2-3 days"
    },
    {
      "type": "text",
      "id": "benefit_2",
      "label": "Benefit 2",
      "default": "Unlimited Revisions"
    },
    {
      "type": "text",
      "id": "benefit_3",
      "label": "Benefit 3",
      "default": "Upload Any Photo"
    },
    {
      "type": "header",
      "content": "Image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#4b5563"
    },
    {
      "type": "color",
      "id": "eyebrow_color",
      "label": "Eyebrow color",
      "default": "#b45309"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title color",
      "default": "#1f2937"
    },
    {
      "type": "color",
      "id": "highlight_color",
      "label": "Highlighted title color",
      "default": "#d4a017"
    },
    {
      "type": "color",
      "id": "button_background",
      "label": "Button background",
      "default": "#d4a017"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text color",
      "default": "#1a1a1a"
    },
    {
      "type": "range",
      "id": "button_border_radius",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Button border radius",
      "default": 8
    },
    {
      "type": "color",
      "id": "benefit_icon_color",
      "label": "Benefit icon color",
      "default": "#d4a017"
    }
  ],
  "presets": [
    {
      "name": "Hero with image"
    }
  ]
}
{% endschema %}
